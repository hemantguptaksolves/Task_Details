<div class="py-3 border-bottom comment comment-<%= comment.id%>">
    <p><%= comment.body %></p>
    <p><%= %></p>
    <div class="btn-group">
        <%= link_to "Delete Comment", post_comment_path(comment.post, comment), method: :delete, data: { confirm: "Are you sure?" }, remote:true, class: "btn btn-sm btn-outline-danger"  %>
    </div>
</div>